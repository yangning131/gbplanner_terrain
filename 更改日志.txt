为了上传至github 将/home/ynp/gbplanner_ws/src/src/sim/rotors_simulator/rotors_description/meshes/m100 
和/home/ynp/gbplanner_ws/src/dog/aliengo_config/maps TIM.ide 压缩了

此为差速度小车 山地探索程序及配置


1。同机器狗类似 某些情况car不走
原因：lookahead 角度差别大  差别大时只转弯 

同机器狗 角度问题
pure_p 降低mypath路径高度
pure_pure 更改跟踪的角度和速度阈值
pure_p 重新计算路径的orientation

2.改回第一版提交的参数

3.扩展树里新的step点再次评价地形； 并且去除每一步斜率超过阈值的点
规划范围  mypath高度  ; terrain_cost 更改  ;pure_p 消除路径更改 增大缩短距离  更改原地转向判断方法 保证原地转圈

4.拟合平面的大小 ； 探索增益的范围 角度

5.显示地形cost  buildgraph加入地形cost 以及地形差别cost及其权重
（以上处理表现很不错 commit -m "terrain_cost and angle add"）

6.选择路径时增益加入地形(evaluate)       commit -m "terrain_cost_evaluatepath_w"        directioncost增大

计划：采样点数据结构  减枝选择父节点时考虑地形，选择最近父节点时 1.欧时距离2.地形cost3。两者的斜率差; djistra搜索时加入地形  选择路径时增益加入地形

首要计划 ：对leaf _vertex 周围车的轮廓做地形分析

探索增益的计算方式要变，三维 不是近视的三维了 (目前只要没有返回点都是unknow 包括打向天空的点)




加入优化部分
1.基于地形生成安全走廊