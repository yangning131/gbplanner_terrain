为了上传至github 将/home/ynp/gbplanner_ws/src/src/sim/rotors_simulator/rotors_description/meshes/m100 
和/home/ynp/gbplanner_ws/src/dog/aliengo_config/maps TIM.ide 压缩了

此为差速度小车 山地探索程序及配置


1。同机器狗类似 某些情况car不走
原因：lookahead 角度差别大  差别大时只转弯 

同机器狗 角度问题
pure_p 降低mypath路径高度
pure_pure 更改跟踪的角度和速度阈值
pure_p 重新计算路径的orientation

2.改回第一版提交的参数

3.扩展树里新的step点再次评价地形； 并且去除每一步斜率超过阈值的点
规划范围  mypath高度  ; terrain_cost 更改  ;pure_p 消除路径更改 增大缩短距离  更改原地转向判断方法 保证原地转圈

4.拟合平面的大小 ； 探索增益的范围 角度

5.显示地形cost  buildgraph加入地形cost 以及地形差别cost及其权重
（以上处理表现很不错 commit -m "terrain_cost and angle add"）

6.选择路径时增益加入地形(evaluate)       commit -m "terrain_cost_evaluatepath_w"        directioncost增大

计划：采样点数据结构  减枝选择父节点时考虑地形，选择最近父节点时 1.欧时距离2.地形cost3。两者的斜率差; djistra搜索时加入地形  选择路径时增益加入地形

首要计划 ：对leaf _vertex 周围车的轮廓做地形分析

探索增益的计算方式要变，三维 不是近视的三维了 (目前只要没有返回点都是unknow 包括打向天空的点)




加入优化部分
1.基于地形生成安全走廊

2.加入端点的判断 好处：更加贴合无点云边际   坏处：点云中的某些空点无法略过 偶尔 造成走廊频繁切换  “commit -m corridor bound”

3.以山地地形的z值约束（走廊的边界查找  参数调试 ） 生成路径 进而生成走廊   此z值是基于向下查找到栅格地图 因为分辨率导致 z值跳动   
拟解决办法  (4)不查找到点云地面的真正z值  查找到拟合平面的z值  合理性————》机器人矩形几何体 真实==优化的z值不能是地面z值  而应该是几何体轮子平面的z值  优化的z值也如此
4.珊格分辨率太低  -》维护 两个地图  高低分辨率     （commit - m"double  maps"）)                 (效果不错  还需增加z点平滑)

5.栅格太多  影响速度   01改成滚动栅格！（commit - m"roll grid  maps"）                          02 height_infitplan改进精确 (不必优化 总耗时1.000ms)

6.增加z点平滑!!!!!!!    good performance  (commit -m "good performance  with mount op")

7.优化运动模型增加差分模型   (commit -m "add difference model")

8.可加可不加：当前后两次迭代的路径变化不大 即z 及斜率稳定 再退出迭代

question:类变量是对象 编译不过   类变量是类的指针

9.添加mpc 更改traj变量；
 整合添加jaclal仿真；  
 添加整合pathserver  ; 
 pathserver add mpc traja；

 10.  launch 文件优化
https://blog.csdn.net/weixin_43879302/article/details/105880972